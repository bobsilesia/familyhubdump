name: Auto Branch Fix

on:
  schedule:
    - cron: "0 2 * * *"
  workflow_dispatch:

jobs:
  autofix-branch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install tools
        run: pip install ruff black isort autoflake

      - name: Run auto fixes
        run: |
          autoflake --remove-all-unused-imports --in-place --recursive .
          ruff check --fix .
          black . --line-length 79
          isort . --line-length 79

      - name: Create PR with changes
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "style: automated formatting (ruff/black/isort)"
          title: "Automated formatting fixes"
          body: "Apply automated formatting per CI policy."
          branch: "autofix/${{ github.run_id }}"
          base: "main"
          labels: "autofix"
