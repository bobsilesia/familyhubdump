# Changelog

## 1.1.2
- **Fix:** Hassfest validation passed (sorted manifest keys, removed core dependencies, fixed service definitions).
- **Fix:** Added missing `strings.json` and `translations` for Config Flow and Options Flow.
- **Fix:** Removed invalid `target: sensor` from global services.
- **Fix:** Standardized on `ruff` for linting/formatting.

## 1.1.x (Failed Attempts)
- Versions 1.1.3 - 1.1.10 were internal fix attempts to resolve Hassfest validation errors and have been removed.

## 1.1.1
- Fixes: Hassfest validation: manifest sorted, services selectors fixed, config schema added.
- CI: standardized on ruff (format/lint), added Dependabot & auto-merge.
